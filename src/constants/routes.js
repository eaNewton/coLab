const routeCodes = {
  INDEX: "/",

  DASHBOARD: "/dashboard",

  PROJECTS: "/projects",

  CREATE: "/create",

  SETTINGS: "/settings"
};

export const routeTypes = {
  PUBLIC: "Public"
};

export default routeCodes;
