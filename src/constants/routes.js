const routeCodes = {
  INDEX: "/",

  DASHBOARD: "/dashboard",

  PROJECTS: "/projects",

  CREATE: "/create",

  SETTINGS: "/settings",
  SETTINGS_ACCOUNT: "/settings/account"
};

export const routeTypes = {
  PUBLIC: "Public"
};

export default routeCodes;
